--if SERVER then
--hook.Add("TTT2SpecialRoleSyncing", "TTT2RoleClownMod", function(ply, tbl)

--		--send all traitors to clown and killer clown
	--	if ply:GetSubRole() == ROLE_CLOWN or ply:GetSubRole() == ROLE_KILLERCLOWN then
--			for p in pairs(tbl) do
	--			if p:GetTeam() == TEAM_TRAITOR then
	--				tbl[p] = {p:GetSubRole(), TEAM_TRAITOR}
	--			end
	--		end
--		end
--	end)
--	end
	